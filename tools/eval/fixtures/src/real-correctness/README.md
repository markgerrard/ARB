Real correctness seed patches live here after Task 10 corpus authoring.

Patch files must apply to the pinned real base, carry no seed/control IDs in comments,
and represent five distinct mechanisms. The builder refuses to run until
ARB_EVAL_REAL_BASE_SHA is pinned.
