# Claude Code harness memory (ARB-related) — portable mirror

The Claude orchestrator's project auto-memory for the codex-arb-artifacts project (Claude
harness format: frontmatter name/description/metadata.type + body, MEMORY-index.md = the
injected index). Mirrored here so a Claude Code orchestrator on ANOTHER server can inherit the
ARB operating memory: copy these files into
`~/.claude/projects/<project-slug>/memory/` (rename MEMORY-index.md -> MEMORY.md) on the new
host, adjusting machine-specific paths where they differ.

Update discipline: the live store is authoritative for THIS machine; re-mirror here when
ARB-relevant memories change (same promotion rule as pi-organic/). Machine-agnostic doctrine
lives in the parent seeds corpus; these carry the Claude-specific framing and cross-links.
