# Pi-authored organic global topics (backup + promotion staging)

These were written BY pi sessions (real session-id provenance preserved) and live primarily in
~/.pi/agent/memory/global/. Mirrored here so they are not single-disk; pi-specific (retrieval
heuristics, orch startup path, capture policy) so NOT auto-seeded to other agents by seed-pi.py
(which only reads the parent directory). Promote any that generalize by moving them up a level.
