#!/usr/bin/env python3
"""Run a digest-pinned validator gate in the gate-runner sandbox."""
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))
from agent_redis_bridge.gate_runner import main  # noqa: E402

if __name__ == "__main__":
    raise SystemExit(main(sys.argv[1:]))
